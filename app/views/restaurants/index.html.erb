<div class="container">
  <body>

    <div class="col-12 col-md-6 offset-md-3">
      <h1 class="mt-3 pt-3 d-flex justify-content-center">These are all my Restaurants</h1>
      <% @restaurants.each do |restaurant| %>
      <p class="d-flex justify-content-center"><%= link_to restaurant.name, restaurant_path(restaurant) %></p>
    <% end %>
    <p class="d-flex justify-content-center bg-success text-white">
      <%= link_to "✚ Add a new restaurant", new_restaurant_path %>
    </p>
    </div>

  </body>
</div>
